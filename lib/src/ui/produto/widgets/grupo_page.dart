import 'package:flutter/material.dart';

class GrupoPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Grupo Atendimento'),
        centerTitle: true,
      ),
    );
  }
}
